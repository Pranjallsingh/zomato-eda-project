# Zomato EDA

This repository contains a beginner-friendly exploratory data analysis (EDA) of the `zomato.csv` dataset. The analysis is provided in the notebook `Sample_EDA_Submission_Template.ipynb` and includes data cleaning, visualizations, and conclusions.

Contents:
- `Sample_EDA_Submission_Template.ipynb` — The completed EDA notebook.
- `zomato.csv` — Dataset (not included in remote by default if you choose to ignore it).
- `requirements.txt` — Python packages required to run the notebook.

How to run:
1. Create a virtual environment: `python -m venv .venv`
2. Activate it: `source .venv/bin/activate` (macOS / Linux) or `\.venv\Scripts\activate` (Windows)
3. Install dependencies: `pip install -r requirements.txt`
4. Start Jupyter and open the notebook: `jupyter notebook Sample_EDA_Submission_Template.ipynb`

If you'd like, I can push this repository to your GitHub account. To allow automatic creation I will use the GitHub CLI (`gh`) if it's installed and authenticated on your machine.
